# UserManagementSystem
The application follows the RESTful API architecture and integrates with a relational database (MySQL) for storing user information securely. It also implements JWT (JSON Web Token) authentication to ensure secure communication between the client and server.
